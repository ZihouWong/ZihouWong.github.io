当需要修改非主工程的时候, 需要先git clone 该工程到本地中, 然后修改Podfile.rb 文件, 使用本地开发, local_pod, 当该库开发完成之后, 需要把该库先提交, 提交合并完成之后, 再改Podfile.rb文件, 最后再提交一遍


cascadingVC 在GuruUIcomponent里面, 主要是负责 竖直方向上的滚动事件

需要做的是在滚动结束的时候,记录contentOffset



采用代理模式 子viewcontroller设计 代理协议，定义协议接口，
父viewcontroller 实现协议接口，实现子viewcontroller 退出时将相关数据更新到父视图

有两个思路

第一个思路是 创建一个共享数据的model, 通过单例模式,使得全局只有一个唯一的实例,然后通过这个实例传递数据

遇到的问题是: 水平滑动和竖直滑动,位于两个不同的工程, 无法同时在两个项目中获取唯一的一个实例

第二个思路是 代理

有三个对象

第一个是协议

协议里包括了 方法的声明, 以及属性的声明

然后是执行者

执行者将协议中的方法实现, 

还有的是代理者, 代理者主要是将



> 定义协议来封装那些需要被委托的功能，这样就能确保遵循协议的类型能提供这些功能

需要委托的功能是保存数据