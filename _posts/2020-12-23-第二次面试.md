---

title: "第二次面试经历："
date: 2020-12-23
categories: 

<!--comments: true-->
---

##问题1：TCP和UDP和HTTP的区别

HTTP:是用于www浏览的一个协议。是在应用层上的协议
TCP、UDP：是机器之间建立连接用的到的一个协议。是在传输层上的协议

1.是否需要连接方面

- TCP需要连接
- UDP不需要连接

2.传输的效果方面

- TCP传输的数据 无差错，无重复，按顺序
- UDP不保证可靠交付

3.传输的对象方面

- TCP面向的是字节流
- UDP面向报文的，没有拥塞控制，当网络发生拥塞时，不会使原主机的发送速率降低，
Ip电话，

4.通信的形式上

- TCP只能是点对点的，
- UDP点对多，点对点，多对多

5.传输的效率上

- TCP传输效率相对较低。
- UDP传输效率高，适用于对高速传输和实时性有较高的通信或广播通信。

##问题2：Swift 和 Objective-C的区别

Swift 语言的优势是：有更高的安全性，更易于阅读，有更高的运行效率，Swift是一门动态类型的强类型语言

Objective-C 语言优势在于，有大量的第三方库，以及资料可以查阅，自由度比swift更高

objective C 面向对象的

Swfit 是面向对象的、面向协议的 

objec

- [ ] Swift 支持extension

拓展1
面向对象的三个基本特征：

封装：封装的意思是 隐藏对象的属性和实现细节，只对外开放接口，以此来控制程序对属性的读写权限。并且把抽象的属性和功能结合成有机的整体

封装的意义：由于封装隐藏了具体的实现，如果实现的改变或升级对于使用方而言是无感知的，提高程序的可维护性；而且封装鼓励程序员把特定数据与对数据操作的功能打包在一起，有利于应用程序的去耦。

继承：继承就是允许创建分等级层次的类。继承就是子类继承父类的特征和行为。使得子类对象具有父类的实例域和方法，或者子类从父类继承方法，使得子类具有和父类相同的行为。

多态：是指同一个行为具有不同的表现形式或者形态的能力。是指一个类实例具有相同的方法，但是在不同的情形下有不同的表现形式。多态机制使得具有不同内部结构的对象可以共享相同的外部接口。这意味着，虽然针对不同对象的具体操作不同，但是通过一个公共的类，他们的哪些操作可以通过相同的方式予以调用

5个原则

单一职责原则： 一个类只应该负责一个工作
开放封闭原则：对象或者实体应该对扩展开放，对修改封闭
里氏替换原则：对父类的调用应该同样适用于子类
依赖倒置原则：低层的实现类应该依赖于高层的抽象类
接口隔离原则：不应该强迫客户端实现一个用不上的接口。in other ward,客户端不应该被迫依赖于一个不使用的方法

拓展1
堆是在程序运行时，程序员申请的某个大小的内存空间，由程序员分配释放， 
栈是在编译器编译时，编译器自动分配释放的，存放函数的参数值，局部变量的值， 栈的速度快

拓展1.1
struct比class更“轻量级” struct分配在栈中，class分配在堆中。


拓展3
##问题3：请说明并比较以下关键词：strong, weak, assign, copy

strong表示 指向并拥有该对象，strong修饰的对象引用计数器+1，只要该对象引用计数器不为0，则不会被销毁。可以将该引用计数器强制设为nil 强行销毁

weak 表示 指向但不拥有该对象。weak修饰的对象引用计数器不会增加。无需手动设置，该对象会自行在内存中销毁

assgin 默认的修饰词 用于修饰需要直接赋值的整型、浮点型等基本数据类型

copy
##问题4：请说一下线程和进程有什么区别


进程是资源分配的最小单位，线程是CPU调度的最小单位
一个进程里可以有多个线程，进程中有公共空间

 - [ ] 线程内的公共空间  

##问题5：请说一下hashmap

hashmap从结构实现来说是 数组。
由于会遇到hash冲突，所以再衍生出数组+链表的结构。
、链表、红黑树实现的

<key,value>键值对的集合


hash冲突的处理办法

1.再哈希

2.链地址 Java8 中当链表个数大于8之后，将链表结构转换为红黑树结构，以

3.建立公共溢出区

4.开放定址法 (h0 + di ) MOD tableLength


##问题6：请说一下同步和异步

同步是线程按顺序执行一系列任务时，需要等待第一项任务完成后，才能开始第二项任务

异步是线程按顺序执行一系列任务时，不需要等待第一项任务完成，就可以开始第二项任务

##问题7：请说一下内存泄漏

内存泄漏
是指程序在申请分配内存空间使用之后，没有进行归还内存的操作，并且自己无法再次使用这部分内存，系统也无法将这部分内存再次分配给需要的程序使用。

内存泄漏 堆积之后会导致 内存溢出

内存溢出 分为 上溢和 下溢

上溢是指内存分配的时候，以及没地方可以分配了。再进行分配操作的话，会使指针指向上级缓存区




##问题8：请说一下数组、链表、树
数组的特点：
把具有相同类型的若干元素按有序的形式组织起来的一种形式
增删慢，查询快

链表的特点：
增删快，查询慢

树的特点

查找特别快
缺点需要维护树的特性
##问题9：请说一下栈和堆


堆是在程序运行时，程序员申请的某个大小的内存空间，由程序员分配释放， 
栈是在编译器编译时，编译器自动分配释放的，存放函数的参数值，局部变量的值， 栈的速度快

##问题10：死锁
起因
禁止抢占（no preemption）：系统资源不能被强制从一个进程中退出。
持有和等待（hold and wait）：一个进程可以在等待时持有系统资源。
互斥（mutual exclusion）：资源只能同时分配给一个行程，无法多个行程共享。
循环等待（circular waiting）：一系列进程互相持有其他进程所需要的资源。

打破互斥条件：允许进程同时访问资源（有些资源就是不可以同时访问的，无实用价值）
打破不可抢占条件：比如给进程设置优先级，高优先级的可以抢占资源(实现困难，降低系统性能)
打破占有且等待条件：实行资源预分配策略，即进程在运行前一次性的向系统申请它所需要的全部资源(不可预测资源的使用，利用率低，降低并发性)
破坏循环等待条件：采用这种策略，即把资源事先分类编号，按号分配，使进程在申请，占用资源时不会形成环路。所有进程对资源的请求必须严格按资源序号递增的顺序提出（限制和编号实现困难，增加系统开销，有些资源暂时不用也需要先申请，增加了进程对资源的占用时间）

#问题：并发和并行
并行都可以是很多个线程，就看这些线程能不能同时被（多个）cpu执行
并发是多个线程被（一个）cpu 轮流切换着执行
#问题11: B树
m = 3
1.根结点至少有两个子节点
2.每个中间节点都包含k个孩子，k-1个元素 其中k的值域是在 [m/2,m]
3.每个子节点都包含k-1个元素
4.所有的子节点都位于同一层
5.每个节点中的元素都是按从小到大排序，节点中k-1个元素正好是k个孩子包含的元素的值域划分

目的是减少树的层数，减少磁盘io的读取次数

B+树

B+树相对于B树有一些自己的优势，可以归结为下面几点。

单一节点存储的元素更多，使得查询的IO次数更少，所以也就使得它更适合做为数据库MySQL的底层数据结构了。
所有的查询都要查找到叶子节点，查询性能是稳定的，而B树，每个节点都可以查找到数据，所以不稳定。
所有的叶子节点形成了一个有序链表，更加便于查找。
三大基本特性：