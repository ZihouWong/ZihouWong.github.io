---

title: "线程池"
date: 2019-09-20 
categories: jekyll update

layout: post
comments: true

---


简单场景

比如说一个网站成为了热点网站，那么对于大量的点击量，就必须要对每一次的点击做出迅速的处理，这样才能达到更好的交互效果…这样就需要多个线程去处理这些请求，以便能够更好的提供服务…

### 优点


* 降低资源消耗。通过重复利用已创建的线程降低线程创建和销毁造成的消耗；
* 提高响应速度。当任务到达时，任务可以不需要等到线程创建就能立即执行；
* 提高线程的可管理性。线程是稀缺资源，如果无限制的创建，不仅会消耗系统资源，还会降低系统的稳定性，使用线程池可以进行统一的分配，调优和监控。

### 使用线程池的流程

* 创建线程池
* 创建任务
* 执行任务
* 关闭线程池



五种线程池的使用场景

---

### 单例线程

一个单线程的线程池，可以用于需要保证顺序执行的场景，并且只有一个线程在执行。

### 缓存线程池

规定能resue(复用)的线程，空闲的时间不能超过60s,一旦超过了60s,就会被移出线程池

### 固定型线程池

一个固定大小的线程池，可以用于已知并发压力的情况下，对线程数做限制。

### 调度型线程池

* 一个可以无限扩大的线程池，比较适合处理执行时间比较小的任务。

* 可以延时启动，定时启动的线程池，适用于需要多个后台线程执行周期任务的场景。

* 一个拥有多个任务队列的线程池，可以减少连接数，创建当前可用cpu数量的线程来并行执行。



github 服务器的ip地址
ip:192.168.1.1


dns解析服务器
wongzihou.cn->192.168.1.1
