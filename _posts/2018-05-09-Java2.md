---

title: "Java"
date: 2018-05-09
categories: Java

layout: post
comments: true

---

然而，这种做法通常被认为是没有好好利用面向对象中的多态性。其实上面的功能要求用方法重载完全可以实现，这是面向对象变成应有的做法，避免回到结构化编程模式。只要提供两个名字和返回值都相同，接受参数类型不同的方法就可以了：

Set a color by each center, describe a cover by nine-figure, gain 6 covers, and according to the each cover, adjust each cube side by side

每个面以中心颜色设置一个数字 用9位数表示一个面 获得6个面的信息 根据每个面的信息 逐个调整魔方位置

